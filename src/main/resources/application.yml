spring:
  profiles:
    active: dev
  resources:
    # 默认资源映射
    add-mappings: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true
  mvc:
    throw-exception-if-no-handler-found: true
  data:
    rest:
      detection-strategy: all
#logging:
#  level:
#    org:
#      hibernate: DEBUG
#    com:
#      zaxxer:
#        hikari:
#         HikariConfig: DEBUG

# 自定义配置
